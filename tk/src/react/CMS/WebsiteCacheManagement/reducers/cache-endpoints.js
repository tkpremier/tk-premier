'use strict'

import { alert as initialState } from '../initial-state'

const cacheEndpoint = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default cacheEndpoint